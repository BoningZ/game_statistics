import service.score_line_service as score_line_service

if __name__ == '__main__':
    # score_line_service.get_list_dates_and_scores_by_publisher_names(["Nintendo", "Xbox", "playstation"])
    # score_line_service.get_list_dates_and_scores_by_platform_names(["Xbox"])
    score_line_service.get_list_dates_and_scores_by_game_names(["Need for speed", "WRC"])


